name: CodeQL

on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: '0 3 * * 1'

permissions:
  security-events: write
  contents: read

jobs:
  analyze:
    uses: github/codeql-action/.github/workflows/codeql.yml@v4
    with:
      languages: javascript
